# Запрос N чисел для работы
n = int(input("Введите количество чисел (N): "))

# INT, которые мы будем считать и выводить в конце
total_sum = 0
even_count = 0
odd_count = 0

# Цикл для ввода N чисел
for i in range(n):
    num = int(input(f"Введите число {i + 1}: "))
    
    # суммируем
    total_sum += num
    
    # Проверяем чётность числа
    if num % 2 == 0:
        even_count += 1
    else:
        odd_count += 1

# Вычисляем среднее арифметическое
if n > 0:
    average = total_sum / n
else:
average = 0  # Если N = 0, среднее равно 0 (чтобы избежать деления на 0)

# Выводим результаты
print("\nРезультаты:")
print(f"Сумма всех чисел: {total_sum}")
print(f"Среднее арифметическое: {average}")
print(f"Количество чётных чисел: {even_count}")
print(f"Количество нечётных чисел: {odd_count}")